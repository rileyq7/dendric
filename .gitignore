# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
*.egg-info/
dist/
build/

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Data & results
data/*.jsonl
results/*.json
results/*.log
*.db
*.sqlite

# Environment
.env
.env.local

# OS
.DS_Store
.AppleDouble
.LSOverride

# Project-specific
legacy/
docs_archive/
data/
results/
tests/
*.jsonl
.pytest_cache/
mnestic-ui.jsx
QUICK_REFERENCE.txt
# Ablation/sweep results: keep the small per-config JSON summaries +
# raw rows tracked so the rigor doc's tables are repo-reproducible,
# but ignore the noisy per-server logs from the legacy bench harness.
ablation_results/server_*.log
ablation_results/results_*.json
.claude/

# Bench output: keep harness code + analysis docs tracked, ignore generated runs.
# Scripts under src/scripts/ ARE tracked. Top-level scripts/ is a different path
# we don't currently use; if it ever exists, decide then.
longmemeval/*.jsonl
longmemeval/*.jsonl.eval-results-*
longmemeval/*.json
longmemeval/*.log
longmemeval/debug/*.json
# Top-level scripts/ — legacy ad-hoc dir, not src/scripts/
/scripts/

# Personal data — Claude exports, any other user-data dumps. Never commit.
data-*.zip
*.export.json
*.export.zip
